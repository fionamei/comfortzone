<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingHorizontal="@dimen/generalMargin"
    android:paddingVertical="@dimen/miniMargin">

    <ImageView
        android:id="@+id/ivCityIcon"
        android:layout_width="@dimen/savedCityIconSize"
        android:layout_height="@dimen/savedCityIconSize"
        android:layout_gravity="center"
        tools:srcCompat="@tools:sample/avatars" />

    <TextView
        android:id="@+id/tvCityName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/generalMargin"
        android:layout_marginTop="@dimen/generalMargin"
        android:layout_weight="1"
        android:textColor="@color/black"
        android:textSize="@dimen/generalTextSize"
        tools:text="New York City" />
</LinearLayout>